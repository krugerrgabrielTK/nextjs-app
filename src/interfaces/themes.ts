export interface IDefaultTheme {
    colors: {
        background: string;
        white: string;
    }
}

export interface ITheme {
    theme: {
        colors: {
            background: string;
            white: string;
        }
    }
}